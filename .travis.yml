language: php

php:
  - 5.3
  - 5.4

before_script:
  - composer --dev install
  - mysql -e 'create database zenmagick;'
  - mysql zenmagick < vendor/zenmagick/zencart/zc_install/sql/mysql_zencart.sql
  # the doctrine schema breaks the original SQL and also means we are missing the
  # default configuration settings
  #- php app/console doctrine:schema:update --force 
  - mysql zenmagick < etc/sql/mysql_demo.sql
