{% extends '@Storefront/default_layout.html.twig' %}
{% block body %}
<?php $view['slots']->set('crumbtrail', $crumbtrail->addCrumb(_zm('Discount Coupon'))); ?>
<?php echo $utils->staticPageContent("discount_coupon") ?>

{{ form.open('discount_coupon', '', false, {'method' : 'get'})|raw }}
  <fieldset>
    <legend>{{ 'Look-up Discount Coupon ...'|trans }}</legend>
    <label for="couponCode">{{ 'Your Code'|trans }}</label>
    <input type="text" id="couponCode" name="couponCode" size="40" value="<?php echo $view->escape($currentCoupon) ?>" />
  </fieldset>
  <div class="btn"><input type="submit" class="btn" value="{{ 'Send'|trans }}" />
</form>
{% endblock %}
