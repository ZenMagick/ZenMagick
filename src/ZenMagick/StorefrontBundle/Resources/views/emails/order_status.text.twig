{{ 'Dear %first_name% %last_name%,'|trans({ '%first_name%' : currentAccount.firstName, '%last_name%' : currentAccount.lastName}) }}


{{ 'This is to inform you that your order #%id% has been updated.'|trans('%id%': currentOrder.id}) }}

<?php if (ZenMagick\StoreBundle\Entity\Account::REGISTERED == $currentAccount->getType()) { ?>
{{ 'More details can be found at the following URL: %account_history_url%'|trans({'%account_history_url%' : $view['router']->generate('account_history_info', array('order_id' => currentOrder.id), true)}) }}
<?php } else { ?>
{{ 'You can check the status of your order at: %guest_history_url%'|trans({'%guest_history_url%' : $view['router']->generate('guest_history', array(), true)}) }}
<?php } ?>

<?php if ($newOrderStatus != $currentOrder->getStatusName()) { ?>
    {{ 'The new order status is: %status%.'|trans({'%status%' : newOrderStatus}) }}
<?php } ?>

<?php if (!empty($comment)) { ?>
{{ 'The following comment has been added to your order:'|trans }}

<?php echo $comment ?>
<?php } ?>


{{ 'Regards, %store_name%'|trans({'%store_name%' : $view['settings']->get('storeName')}) }}

<?php echo strip_tags($utils->staticPageContent('email_advisory')) ?>
