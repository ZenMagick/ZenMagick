<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html lang="<?php echo $language->getCode() ?>">
<head>
<title>{{ 'A gift certificate from %store_name%'|trans({'%store_name%' : $view['settings']->get('storeName')}) }}</title>
</head>
<body>
<body>
<div style="font-family:Verdana,Arial,Helvetica,sans-serif;font-size:10pt;">
<?php if (empty($htmlMessage)) { ?>
<p>{{ 'We're pleased to offer you a Gift Certificate'|trans }}</p>
<?php } else { ?>
<div><?php echo $message; ?></div>
<?php } ?>

<p>{{ 'You have been sent a Gift Certificate worth %amount%.'|trans({'%amount%' : utils.formatMoney(currentCoupon.amount, false)}) }}</p>
<p>{{ 'The code to redeem your Gift Certificate is: %code%.'|trans({'%code%' : currentCoupon.code}) }}</p>
<p>{{ 'To redeem your gift, visit < href="%redeem_url%">%store_name%</a>'|trans({'%redeem_url%':  url('gv_redeem, {'couponCode' : currentCoupon.code}), '%store_name%' : $view['settings']->get('storeName')}) }}</p>

<?php if (!isset($isSupressDisclaimer)) { ?>
    {{ utils.staticPageContent('email_advisory')|raw }}
<?php } ?>
<?php echo $office_only_html; ?>
</div>
</body>
</html>
