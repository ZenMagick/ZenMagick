{% extends '@Storefront/default_layout.html.twig' %}
{% block body %}
    {% set page = app.request.query.get('cat') %}
    <?php $view['slots']->set('crumbtrail', $crumbtrail->addCrumb(page|humanize)); ?>
    {{ utils.staticPageContent(page)|raw }}
{% endblock %}
