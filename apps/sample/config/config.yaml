#
# default settings
#

zenmagick:
  mvc:
    templates:
      ext: .tpl
    seo:
      type: realpath

  core:
    locales:
      provider: YamlLocale

  base:
    events:
      listeners:
        - ZMSampleEventHandler
