#
# default settings
#

zenmagick:
  mvc:
    templates:
      ext: .tpl

  core:
    locales:
      provider: ZMYamlLocale

  base:
    events:
      listeners:
        - zenmagick\http\BootstrapListener
        - ZMSampleEventHandler
  http:
    request:
      urlType: realpath
