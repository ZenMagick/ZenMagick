#
# default settings
#

zenmagick:
  mvc:
    templates:
      ext: .tpl
    seo:
      type: realpath

  core:
    locales:
      provider: ZMYamlLocale

  base:
    events:
      listeners:
        - zenmagick\http\BootstrapListener
        - ZMSampleEventHandler
