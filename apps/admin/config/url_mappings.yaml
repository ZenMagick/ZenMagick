#
# ZenMagick admin URL mappings
#
#

## global mappings
global:
  success:
    view: ZMRedirectView
  error:
    template: error
    layout: 
  success-demo:
    view: ZMRedirectView#requestId=index
  store-view:
    view: ZMSavantView

## page mappings    
page:
  login:
    success:
      view: ZMRedirectView#requestId=index
    logged-in:
      view: ZMRedirectView#requestId=index
  logoff:
    success:
      view: ZMRedirectView#requestId=login
  reset_password:
    success:
      view: ZMRedirectView#requestId=login
  update_user:
    formId: updateUser
    form: ZMAdminUserForm
    success:
      view: ZMRedirectView#requestId=index
  edit_admin_user:
    formId: adminUser
    form: ZMAdminUserForm
    success:
      view: ZMRedirectView#requestId=admin_users
  cache_admin:
    success:
      view: ZMRedirectView#requestId=cache_admin
  plugins:
    success-install:
      view: ZMRedirectView#requestId=plugins
    success-uninstall:
      view: ZMRedirectView#requestId=plugins
    success-upgrade:
      view: ZMRedirectView#requestId=plugins
    plugin-conf:
      layout: 
      template: plugin_conf
  legacy-config:
    success:
      view: ZMRedirectView#requestId=legacy-config
