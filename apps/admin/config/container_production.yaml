imports:
    ## app container
    - { resource: container.yaml }
    ## shared services
    - { resource: ../../../config/store-services.yaml }
    ## shared config
    - { resource: ../../../config/store-container.yaml }
    ## shared production container
    - { resource: ../../../config/store-container_production.yaml }
    ## custom shared container
    - { resource: local-container.yaml, mode: append }
    ## custom production container
    - { resource: local-container_production.yaml, mode: append }
