meta:
  name: 'DataExporter'
  version: '${plugin.version}'
  author: 'zenmagick.org'
  description: 'Export data from the database.'

  context: admin

admin:
  router:
    page:
      export_orders:
        controller: ZenMagick\plugins\dataExporter\controller\ExportOrdersController
  menu:
    data-exporter:
      parent: tools
      name: 'Data Export'
    export-orders:
      parent: data-exporter
      requestId: export_orders
      name: 'Orders'
