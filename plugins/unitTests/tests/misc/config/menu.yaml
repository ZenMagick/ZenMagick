# root items
index: { requestId:dashboard, name:Dashboard }
catalog: { requestId:catalog, name:Catalog }
tools: { requestId:tools, name:Tools }
configuration: { requestId:configuration, name:Configuration }

# tools
tools-cms: { parent:tools, name:Manage Content }
static-content: { parent:tools-cms, requestId:content_editor, name:Static Content }
ezpages: { parent:tools-cms, requestId:ezpages, name:EZPages }
