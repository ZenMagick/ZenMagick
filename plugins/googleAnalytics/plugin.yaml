meta:
  name: 'Google Analytics'
  version: '${plugin.version}'
  author: 'zenmagick.org'
  description: 'Adds Google Analytics.'

  context: storefront
  options:
      properties:
          uacct:
              name: Account
              description: 'Your Google Analytics account number'
              type: text
          affiliation:
              name: 'Store affiliation'
              description: 'Optional partner or store affiliation'
              type: text
          conversionId:
              name: 'AdWords Conversion id'
              description: 'Optional AdWords conversion id (leave empty to ignore)'
              type: text
          conversionLang:
              name: 'AdWords Conversion language'
              description: 'Optional AdWords conversion language'
              type: text
          identifier:
              name: 'Product identifier'
              description: 'Select whether to use productId or model to identify products'
              type: select
              config:
                  options:
                      productId: 'Product Id'
                      model: Model
                  style: select
          pagenameFormat:
              name: 'Page name format'
              description: 'Select the formt to use to track individual URLs'
              type: select
              config:
                  options:
                      pagename: Pagename
                      uri: Uri
                      custom: Custom
                      none: None
                  style: select
          address:
              name: 'Transaction Address'
              description: 'Select which address to use for transaction (order) logging'
              type: select
              config:
                  options:
                      shipping: Shipping
                      billing: Billing
                  style: select
          debug:
              name: Debug
              description: 'Generate code, but make inactive.'
              type: boolean
              config:
                  default: true
                  label: Debug
                  style: checkbox
