meta:
  name: 'Autoresponder'
  version: '${plugin.version}'
  author: 'zenmagick.org'
  description: 'Auto emails based on acccount events.'

  context: storefront
  options:
      properties:
          config:
              name: Config
              description: 'Config data'
              type: text
              config:
                  default: 'a:0:{}'
